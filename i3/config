set $mod Mod4
set $alt Mod1

font pango:Iosevka Nerd Font Mono Medium 10
# font pango:JetBrainsMono Nerd Font Mono Medium 10

exec --no-startup-id redshift
exec --no-startup-id xset -b
exec --no-startup-id lxpolkit
exec --no-startup-id xset r rate 200 35
exec --no-startup-id picom --config ~/.config/picom/picom.conf
exec --no-startup-id setxkbmap -model abnt2 -layout br -variant abnt2

# exec --no-startup-id picom --config ~/.config/picom/picom-blur.conf
# exec --no-startup-id autotiling
# exec --no-startup-id conky

# Atalhos de vídeo (HDMI)
bindsym $mod+Shift+m exec xrandr --output HDMI-A-0 --same-as eDP --auto
bindsym $mod+Shift+v exec xrandr --output HDMI-A-0 --right-of eDP --auto
bindsym $mod+Shift+x exec xrandr --output HDMI-A-0 --off

# Papel de parede
exec --no-startup-id feh --bg-scale ~/wallpapers/nature.jpg

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

bindsym XF86MonBrightnessUp exec brightnessctl set +10%
bindsym XF86MonBrightnessDown exec brightnessctl set 10%-

# Controles de Mídia
bindsym XF86AudioPlay exec --no-startup-id playerctl play-pause
bindsym XF86AudioPause exec --no-startup-id playerctl play-pause
bindsym XF86AudioNext exec --no-startup-id playerctl next
bindsym XF86AudioPrev exec --no-startup-id playerctl previous

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

tiling_drag modifier titlebar

# scratchpad
bindsym $mod+Shift+Tab move scratchpad
bindsym $mod+Tab scratchpad show

# kill focused window
bindsym $mod+q kill

#menu
# bindsym $Mod+d exec --no-startup-id j4-dmenu-desktop --dmenu="dmenu -i  -fn 'Iosevka Nerd Font Mono:size=10'"

bindsym $mod+d exec --no-startup-id "rofi -show combi"
set $up k
set $down j
set $left h
set $right l

# >>> container focus <<<
bindsym $mod+$left focus left
bindsym $mod+$down focus down
bindsym $mod+$up focus up
bindsym $mod+$right focus right

# >>> move containers <<<
bindsym $mod+Shift+$left move left
bindsym $mod+Shift+$down move down
bindsym $mod+Shift+$up move up
bindsym $mod+Shift+$right move right


# split in horizontal orientatio
bindsym $mod+z split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (staced, tabbed, toggle split)
bindsym $mod+s layout stacking
bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# change focus between tiling / floating windows
bindsym $mod+space focus mode_toggle

# focus the parent container
bindsym $mod+a focus parent

# focus the child container
bindsym $mod+Shift+d focus child

set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload

# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart

# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec i3-msg exit

# launch obsidian
bindsym $mod+Shift+o exec flatpak run md.obsidian.Obsidian

# launch zapzap
bindsym $mod+Shift+z exec flatpak run com.rtosta.zapzap

# launch firefox
bindsym $mod+Shift+w exec firefox

# launch bluetooth
bindsym $mod+Shift+b exec blueman-manager

# launch files
bindsym $mod+Shift+f exec thunar

# start a terminal
bindsym $mod+Return exec xfce4-terminal

# floating terminal
bindsym $mod+Shift+Return exec xfce4-terminal --title="floating_term"

# gerenciador de tarefas
# bindsym $mod+m exec xfce4-terminal --command btop

# resize window (you can also use the mouse for that

bindsym $mod+r mode "resize"
mode "resize" {

         # same bindings, but for the arrow keys
        bindsym $left resize shrink width 10 px or 10 ppt
        bindsym $down resize grow height 10 px or 10 ppt
        bindsym $up resize shrink height 10 px or 10 ppt
        bindsym $right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

# Open softwares
for_window [title="floating_term"] floating enable
for_window [class="Thunar"] floating enable
for_window [class="Blueman-manager"] floating enable
for_window [title="Asteroides"] floating enable
for_window [class="org.gnome.FileRoller"] floating enable

# [title=""]

bindsym Print exec flameshot gui

bindsym Shift+Print exec ~/scripts/capture_ocr.sh

bindsym $alt+Tab exec --no-startup-id rofi -show window

bindsym $alt+l exec i3lock --nofork --show-failed-attempts -i ~/wallpapers/173801.png

bindsym $alt+Shift+w exec sh -c 'pgrep -x nm-applet >/dev/null && pkill -x nm-applet || nm-applet'

bindsym $alt+Shift+s exec ~/.config/i3status/toggle_bar.sh

bindsym $alt+Shift+b exec sh -c 'pgrep -x blueman-applet >/dev/null && pkill -x blueman-applet || blueman-applet'
bindsym $alt+z exec boomer

# bindsym $mod+$alt+m exec i3-input -F 'mark %s' -P 'Mark name: '
# bindsym $mod+Shift+u unmark
# bindsym $mod+m exec i3-input -F '[con_mark="%s"] focus' -P 'Go to mark: '

# bindsym $alt+z exec boomer &  xsetroot -cursor_name left_ptr

# ============================================================
# Gaps
# gaps inner 3
# gaps outer 3
#
default_border pixel 3
default_floating_border pixel 3
#
# # Gaps inteligentes
# smart_gaps on
smart_borders on

# ============================================================

# bar {
#         status_command i3blocks -c ~/.config/i3blocks/config
#
# }

bar {
    status_command i3status
}

focus_follows_mouse yes
hide_edge_borders both
